const form=document.querySelector("#estimate");form?.addEventListener("submit",e=>{e.preventDefault();document.querySelector("#thanks").hidden=false});const menu=document.querySelector(".mobile-menu");const nav=document.querySelector(".nav nav");menu?.addEventListener("click",()=>{const open=nav.classList.toggle("open");menu.textContent=open?"×":"☰";menu.setAttribute("aria-expanded",String(open))});nav?.querySelectorAll("a").forEach(a=>a.addEventListener("click",()=>{nav.classList.remove("open");if(menu)menu.textContent="☰"}));